import { styled } from 'styled-components';

export const HomeStyled = styled.div`
  text-align: left;
  padding: 96px 0 32px 0;
  min-height: calc(100vh - 32px);
`;

export const ClocksContainer = styled.div`
  margin-top: 32px;
`;
