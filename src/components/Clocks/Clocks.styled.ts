import { Alert } from '@mui/material';
import { styled } from 'styled-components';

export const ClocksStyled = styled.div``;

export const AlertCustom = styled(Alert)`
  width: 100%;
  margin-left: 16px;
`;
